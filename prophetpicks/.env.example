# API-Football is used by local/server-side sync jobs only.
# Do not prefix this with VITE_; Vite env vars are exposed to the browser.
APIFOOTBALL_KEY=

# The Odds API key for the /api/odds Vercel route. Server-side only.
# Leave blank to keep the Odds Board running on deterministic local demo data.
ODDS_API_KEY=
